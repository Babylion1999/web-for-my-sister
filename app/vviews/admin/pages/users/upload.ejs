<% 
const collection = "users";
const linkPrefix = systemConfig.prefixAdmin + `/${collection}/`;
%>
<% include ./../../elements/errors %>
<div class="container mt-3">
    <h2>Custom File</h2>
    <p>To create a custom file upload, wrap a container element with a class of .custom-file around the input with type="file". Then add the .custom-file-input to the file input:</p>
    <form action="<%= linkPrefix %>upload/" method="POST" enctype="multipart/form-data">
      
      
      <p>Default file:</p>
      <input type="file" id="myFile" name="avatar">
    
      <div class="mt-3">
        <button type="submit" class="btn btn-primary">Submit</button>
      </div>
    </form>
  </div>
  
  <script>
  //Add the following code if you want the name of the file appear on select
  
  </script>